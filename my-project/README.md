Background Changer - React + Tailwind CSS v4
A simple yet elegant React application that allows users to change the background color of the page dynamically using a set of interactive buttons. This project demonstrates state management in React and modern styling with Tailwind CSS.

🚀 Features
Real-time State Update: Uses React's useState hook to manage and update the background color instantly.

Modern Styling: Built with Tailwind CSS v4, utilizing the latest @import "tailwindcss"; configuration.

Responsive Design: A centered navigation bar at the bottom of the screen that works on all devices.

Interactive UI: Smooth transitions and clean button designs with shadow effects.

🛠️ Tech Stack
Framework: React.js (Vite)

Styling: Tailwind CSS v4

State Management: React Hooks (useState)

📋 Prerequisites
Before running this project, ensure you have the following installed:

Node.js (Latest version)

npm (Node Package Manager)

